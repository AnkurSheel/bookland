@inherits StatiqRazorPage<IDocument>

<!DOCTYPE html>
<html lang="en" class="min-h-screen">
<head>
    <title>@Document.GetString("title")</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <link href="/assets/styles.css" rel="stylesheet"/>
</head>
<body class="font-sans font-normal leading-relaxed font-base text-gray-800 min-h-screen">
<header>
    @await Html.PartialAsync("partials/_Navigation.cshtml")
</header>
<main class="mx-auto prose-lg max-w-prose p-8">
    <div class="flex flex-col min-h-screen">
        <article>
            <h1 class="text-center">@Document.GetString("title")</h1>
            @RenderBody()
        </article>
    </div>
</main>

@await Html.PartialAsync("partials/_Scripts.cshtml")
@await RenderSectionAsync("Scripts", false)

</body>
</html>
